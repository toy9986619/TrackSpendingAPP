<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <DatePicker
        android:id="@+id/datePicker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="8dp"
        android:layout_marginRight="8dp"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginLeft="8dp"
        app:layout_constraintLeft_toLeftOf="parent"
        android:datePickerMode="spinner"
        android:calendarViewShown="false"
        app:layout_constraintVertical_bias="0.671" />

    <Button
        android:id="@+id/popup_okButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="-29dp"
        android:layout_marginTop="7dp"
        android:onClick="popupOkButton"
        android:text="確定"
        app:layout_constraintLeft_toLeftOf="@+id/datePicker"
        app:layout_constraintTop_toBottomOf="@+id/datePicker" />

    <Button
        android:id="@+id/popup_cancelButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="-52dp"
        android:layout_marginTop="-7dp"
        android:onClick="popupCancelButton"
        android:text="取消"
        app:layout_constraintRight_toRightOf="@+id/datePicker"
        app:layout_constraintTop_toTopOf="@+id/popup_okButton" />

</android.support.constraint.ConstraintLayout>