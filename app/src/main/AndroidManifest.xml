<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tsaujt.finalproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/MyAppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddActivity"
            android:theme="@style/MyAppTheme"/>
        <activity
            android:name=".PopUpTimeChoose"
            android:theme="@style/AppTheme.PopUpTime" />
        <activity android:name=".ShowDBActivity"
            android:theme="@style/MyAppTheme"/>
        <activity android:name=".EditActivity"
            android:theme="@style/MyAppTheme"/>
        <activity
            android:name=".ShowRecordActivity"
            android:label="@string/title_activity_show_record"

            android:theme="@style/MyAppTheme.NoActionBar" />
        <activity android:name=".LoginActivity"
            android:theme="@style/MyAppTheme" />


        <activity android:name=".SignUpActivity"></activity>

    </application>

</manifest>